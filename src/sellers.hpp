#ifndef SELLERS_H
#define SELLERS_H 

#include <vector>
#include <string>

using namespace std;

namespace sellers {

void build(const vector<string> &pats, int r);
int search(const char *txt);

} //namespace sellers
#endif
