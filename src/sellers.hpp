#ifndef SELLERS_H
#define SELLERS_H 
#include <vector>
#include <string>

namespace sellers {

void build(const vector<string> &pats, int r);
int search(const string &txt);

} //namespace sellers
#endif
